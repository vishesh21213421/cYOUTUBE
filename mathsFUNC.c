#include <stdio.h>
#include <math.h>

int main()
{
    float x=45;
     //  x=sqrt(x);  // square root
     //  x=pow(x,5); // x^5
     //  x=round(x); // round to nearest integer
     //  x=ceil(x);   // round up
    //  x=floor(x);  // round down
     // x=abs(x);   // distance from zero
     // x=log(x);   // natural logarithm
     //x=sin(x);   // sine of x (x in radians)
     //x=cos(x);   // cosine of x (x in radians)
        x=tan(x);   // tangent of x (x in radians)
     printf("%f",x);








    
    return 0;
}