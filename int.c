#include <stdio.h>
int main(){

    // variable = a reusable container for a value behaves as 
    // if it were the value it contains

    int age=25;
    int year=2025;
    int quantity =2;
    printf("you are %d years old\n", age);
    printf("the year is %d\n",year);
    printf("you have ordered %d x items\n",quantity);


    
}